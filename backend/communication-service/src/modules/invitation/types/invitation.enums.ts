export enum INVITATION_STATUS {
  PENDING = 'PENDING',
  ACCEPTED = 'ACCEPTED',
  DECLINED = 'DECLINED',
}

export enum INVITATION_SUCCESS_MESSAGES_LABEL {
  LISTED = 'Invitations retrieved successfully',
  ACCEPTED = 'Invitation accepted',
  DECLINED = 'Invitation declined',
}
